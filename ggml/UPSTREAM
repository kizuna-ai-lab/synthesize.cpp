repo: https://github.com/ggml-org/ggml
sha:  707321c4cf6d21cb4bc831aa8b687dbf01a521ce

This directory is upstream's tracked tree at the SHA above, minus .github/,
plus the local patches listed below (see ggml-patches/README.md for what each
one is and why). Do not edit files here by hand: change a patch file instead,
then run scripts/sync-ggml.sh with no arguments to regenerate this directory,
and commit both together. A sync aborts if a patch stops applying, so local
changes are never silently lost.

patches applied:
  - 0001-synthesize-local.patch
